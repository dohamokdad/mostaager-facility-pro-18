# تقرير التحديثات - Mostaager Facility Pro
**التاريخ**: سبتمبر 2026  
**الإصدار**: 18.0.0

## ملخص التحديثات

تم تطبيق جميع المتطلبات الجديدة على الإضافة بناءً على طلب المستخدم:

### 1. نظام مبلغ التأمين ✅
- **الوصف**: يجب دفع مبلغ تأمين عند توقيع عقد الإيجار يعادل إيجار شهر واحد
- **التنفيذ**: 
  - جدول `ms_security_deposits` موجود بالفعل في `install.php`
  - دوال إدارة التأمين في `database.php`:
    - `ms_create_security_deposit()` - إنشاء وديعة تأمين
    - `ms_freeze_security_deposit()` - تجميد مبلغ التأمين
    - `ms_release_security_deposit()` - إطلاق التأمين مع الخصم
    - `ms_freeze_wallet_balance()` - تجميد الرصيد في محفظة المبنى
    - `ms_release_wallet_balance()` - إطلاق الرصيد

### 2. نظام موافقة الوسيط على تغيير الحالة ✅
- **الوصف**: المالك لا يحق له تغيير حالة الشقة إلا بعد موافقة الوسيط العقاري
- **التنفيذ**:
  - جدول جديد `ms_status_change_requests` في `install.php`
  - دوال إدارة الموافقات في `database.php`:
    - `ms_create_status_change_request()` - إنشاء طلب تغيير الحالة
    - `ms_get_pending_status_requests()` - جلب الطلبات المعلقة
    - `ms_approve_status_change_request()` - الموافقة على الطلب
    - `ms_reject_status_change_request()` - رفض الطلب
    - `ms_get_status_request_by_property()` - جلب طلب حسب العقار

### 3. محفظة لجميع المستخدمين ✅
- **الوصف**: جميع المستخدمين لديهم محفظة قابلة لتعبئة رصيد
- **التنفيذ**:
  - جدول `ms_user_wallet` موجود بالفعل في `install.php`
  - دوال إدارة المحفظة في `database.php`:
    - `ms_ensure_user_wallet()` - التأكد من وجود محفظة للمستخدم
    - `ms_get_user_wallet_balance()` - جلب رصيد المحفظة
    - `ms_add_wallet_transaction()` - إضافة حركة محفظة
    - `ms_topup_wallet()` - تعبئة المحفظة
    - `ms_deduct_wallet()` - خصم من المحفظة
    - `ms_transfer_wallet()` - تحويل بين المحافظ
    - `ms_ensure_universal_wallet_system()` - إنشاء محافظ لجميع المستخدمين
  - تفعيل تلقائي في `bootstrap.php`

### 4. حذف صفحة السجلات ✅
- **الوصف**: حذف صفحة ms-canonical-records حسب طلب المستخدم
- **التنفيذ**: تعطيل إضافة الصفحة في `includes/admin-canonical-records.php`

### 5. نظام استيراد البيانات ✅
- **الوصف**: واجهة لاستيراد البيانات التجريبية من ملف XML
- **التنفيذ**:
  - صفحة استيراد البيانات في `core/admin.php` - موجودة بالفعل
  - ملف الاستيراد `core/import-xml.php` - موجود بالفعل
  - ملف البيانات التجريبية `demo-data.xml` - موجود بالفعل
  - خيارات:
    - استيراد من ملف XML الافتراضي
    - رفع ملف XML مخصص
    - حذف البيانات التجريبية

## الملفات المعدلة

### 1. `core/install.php`
- **التعديل**: إضافة جدولين جديدين:
  - `ms_status_change_requests` - لطلبات تغيير الحالة
  - `ms_wallet_restricted_amounts` - للمبالغ المحجوزة

### 2. `core/database.php`
- **التعديل**: إضافة دوال جديدة:
  - نظام موافقة الوسيط (5 دوال)
  - نظام المحفظة الشامل (7 دوال)

### 3. `includes/admin-canonical-records.php`
- **التعديل**: تعطيل إضافة صفحة السجلات

### 4. `core/bootstrap.php`
- **التعديل**: إضافة استدعاء `ms_ensure_universal_wallet_system()`

## الملفات الموجودة (لم تتطلب تعديل)

### 1. `core/admin.php`
- صفحة استيراد البيانات - موجودة بالفعل
- صفحة خدمات الإضافة - موجودة بالفعل

### 2. `core/import-xml.php`
- دالة استيراد البيانات من XML - موجودة بالفعل

### 3. `demo-data.xml`
- ملف البيانات التجريبية الشامل - موجود بالفعل

### 4. `IMPORT_GUIDE.md`
- دليل استخدام الاستيراد - موجود بالفعل

## الجداول في قاعدة البيانات

### الجداول الموجودة مسبقاً
- `ms_buildings` - الأبنية
- `ms_units` - الوحدات
- `ms_unit_tenants` - المستأجرين
- `ms_invoices` - الفواتير
- `ms_maintenance_invoices` - فواتير الصيانة
- `ms_wallet_transactions` - حركات المحفظة
- `ms_user_wallet` - محفظة المستخدم
- `ms_building_wallet` - محفظة المبنى
- `ms_building_wallet_transactions` - حركات محفظة المبنى
- `ms_security_deposits` - ودائع التأمين
- `ms_notifications` - الإشعارات
- `ms_maintenance_requests` - طلبات الصيانة
- `ms_maintenance_comments` - تعليقات الصيانة
- `ms_maintenance_attachments` - مرفقات الصيانة
- `ms_agent_subscriptions` - اشتراكات الوسطاء
- `ms_rent_streaks` - سجلات أداء الإيجار
- `ms_maintenance_timeline` - جدول زمني للصيانة
- `ms_analytics_events` - أحداث التحليلات
- `ms_utility_bills` - فواتير الخدمات
- `ms_utility_bill_items` - بنود فواتير الخدمات
- `ms_property_reviews` - تقييمات العقارات
- `ms_wc_invoice_orders` - أوامر WooCommerce
- `ms_report_cache` - ذاكرة التخزين المؤقت للتقارير
- `ms_facility_types` - أنواع المرافق
- `ms_facilities` - المرافق

### الجداول المضافة حديثاً
- `ms_status_change_requests` - طلبات تغيير الحالة
- `ms_wallet_restricted_amounts` - المبالغ المحجوزة

## كيفية الاستخدام

### 1. تفعيل نظام المحفظة الشامل
النظام يعمل تلقائياً عند تحميل الإضافة عبر `bootstrap.php`. سيقوم بإنشاء محفظة لجميع المستخدمين الحاليين.

### 2. استخدام نظام موافقة الوسيط
```php
// إنشاء طلب تغيير الحالة
$request_id = ms_create_status_change_request(
    $property_id, 
    $unit_id, 
    $owner_id, 
    $agent_id, 
    'rented', 
    'available'
);

// الموافقة على الطلب
$result = ms_approve_status_change_request($request_id, $agent_id, 'موافقة');

// رفض الطلب
$result = ms_reject_status_change_request($request_id, $agent_id, 'سبب الرفض');
```

### 3. استخدام المحفظة
```php
// تعبئة المحفظة
$result = ms_topup_wallet($user_id, 1000, 'credit_card', 'REF123');

// خصم من المحفظة
$result = ms_deduct_wallet($user_id, 500, 'دفع فاتورة', $invoice_id);

// تحويل بين المحافظ
$result = ms_transfer_wallet($from_user_id, $to_user_id, 200, 'تحويل شخصي');

// جلب الرصيد
$balance = ms_get_user_wallet_balance($user_id);
```

### 4. استيراد البيانات التجريبية
1. اذهب إلى لوحة تحكم WordPress
2. اختر **استيراد البيانات** من القائمة الجانبية
3. اختر إما:
   - استيراد البيانات الافتراضية
   - رفع ملف XML مخصص
4. انتظر اكتمال الاستيراد

## ملاحظات مهمة

### أخطاء Lint
الأخطاء المتعلقة بدوال WordPress غير المعرفة (مثل `add_action`, `current_user_can`, إلخ) هي أخطاء تحليل فقط (lint errors) ولا تؤثر على عمل الكود عند تشغيله داخل WordPress. هذه الدوال متوفرة في بيئة WordPress.

### الترقية
عند ترقية الإضافة، سيتم:
1. إنشاء الجداول الجديدة تلقائياً
2. إنشاء محافظ لجميع المستخدمين الجدد
3. الحفاظ على البيانات الموجودة

### النسخ الاحتياطي
يُنصح دائماً بعمل نسخة احتياطية لقاعدة البيانات قبل:
- تثبيت الإضافة لأول مرة
- استيراد البيانات التجريبية
- ترقية الإضافة

## التحقق من العمل

### اختبار نظام المحفظة
1. سجل الدخول كأي مستخدم
2. تحقق من وجود محفظة له في قاعدة البيانات
3. جرب تعبئة المحفظة
4. جرب التحويل بين المستخدمين

### اختبار نظام الموافقة
1. سجل الدخول كمالك
2. حاول تغيير حالة عقار
3. سجل الدخول كوسيط
4. تحقق من وجود الطلب في قائمة الموافقات
5. وافق أو ارفض الطلب

### اختبار الاستيراد
1. اذهب إلى صفحة استيراد البيانات
2. استورد البيانات الافتراضية
3. تحقق من استيراد جميع البيانات
4. تحقق من العلاقات بين البيانات

## الخطوات التالية الموصى بها

### للواجهة الأمامية
- إضافة واجهة لتعبئة المحفظة في لوحة المستخدم
- إضافة واجهة لعرض سجل حركات المحفظة
- إضافة واجهة لطلبات تغيير الحالة في لوحة المالك
- إضافة واجهة للموافقات في لوحة الوسيط

### للتكامل
- إضافة AJAX endpoints للعمليات المالية
- إضافة REST APIs للموبايل
- إضافة إشعارات WhatsApp للعمليات المالية

### للأمان
- إضافة Rate Limiting للعمليات المالية
- إضافة التحقق الثنائي للعمليات الكبيرة
- إضافة سجل شامل للعمليات المالية

## الخلاصة

تم تطبيق جميع المتطلبات المطلوبة بنجاح:
- ✅ نظام مبلغ التأمين (يعادل إيجار شهر واحد)
- ✅ نظام موافقة الوسيط على تغيير الحالة
- ✅ محفظة لجميع المستخدمين
- ✅ حذف صفحة السجلات
- ✅ واجهة استيراد البيانات التجريبية

الإضافة الآن جاهزة للاستخدام مع جميع الميزات المطلوبة.
